#ifndef __CONSUMER_H
#define __CONSUMER_H
#include ".h"
#include <.h>

void consumer_routine(){
  /*rajouter le sémaphore pour voir si il y a des éléments disponibles*/
  /*rajouter un mutex pour être sûr d'être le seul a y toucher */
  uint8_t tab32 = malloc(sizeof())
  if (bool reversehash(const uint8_t , char *res, size_t len);){
    printf("%s\n", res);
  }
  tab_circulaire->in = tab_circulaire->in+1 % tab_circulaire->length
  /*envoyer un signal a l'autre sémaphore
  libérer le mutex.*/
}
#endif 
